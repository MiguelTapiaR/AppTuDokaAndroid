<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.panteranegra.tudoka">

    <uses-permission android:name="android.permissioin.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DrawImageActivity"></activity>
        <activity android:name=".VistaItemActivity" />
        <activity android:name=".ResumenItemsActivity" />
        <activity android:name=".AgregarItemDevolucionActivity" />
        <activity android:name=".DatosTransporteDevolucionActivity" />
        <activity android:name=".MostrarActivity" />
        <activity android:name=".DatosActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".DatosEnvioActivity" />
        <activity android:name=".MostrarDatosEnvioActivity" />
        <activity android:name=".AgregarItemEnvioActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.Prueba" />
    </application>

</manifest>